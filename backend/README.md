# Backend

Folder placeholder untuk kode backend.

Taruh server API, skrip server, atau kode terkait backend (mis. Express, FastAPI, Django) di sini.

Contoh struktur rencana sii
- `src/` - kode sumber backend
- `Dockerfile` - containerization
- `requirements.txt` / `package.json` - dependency
