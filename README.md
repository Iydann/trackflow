# TrackFlow

This project is TrackFlow — a small Vue 3 + Vite app for demonstrating video upload, preview and counting-line definition flows. It uses plain CSS (no Tailwind) for styling.

The project uses Vue 3 `<script setup>` SFCs. See the Vue docs for more details.